schemaVersion: 1.0
# Example endpoint configuration
endpoints:
  - name: my-service-endpoint
    service:
      port: 8080  # replace <SERVICE_PORT> with an actual port number (e.g., 8080)
    type: REST
